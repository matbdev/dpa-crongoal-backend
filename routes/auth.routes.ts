/*
Configure os endpoints no Hono apontando para o seu controlador.
POST /auth/register (Para o formulário de criar conta).
POST /auth/login (Para o formulário de entrar).
GET /auth/google (Gera o link oficial do Google e redireciona o usuário para lá).
GET /auth/google/callback (A URL de retorno onde o Google devolve o usuário após o login).
*/